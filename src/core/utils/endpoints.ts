const apiUrl = process.env.REACT_APP_API_URL
const staticUrl = process.env.REACT_APP_SOURCES
const coreUrl = process.env.REACT_APP_CORE

export const urlLogin = `${apiUrl}/login`
export const urlAccount = `${apiUrl}/account`

export const urlResource = `${staticUrl}/`
export const urlCore = `${coreUrl}`

